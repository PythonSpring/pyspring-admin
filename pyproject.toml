[project]
name = "PySpring-Modules"
version = "1.0.0"
description = "Default template for PDM package"
authors = [
    {name = "William Chen", email = "ow6201231@gmail.com"},
]
dependencies = []
requires-python = ">=3.10"
readme = "README.md"
license = {text = "MIT"}

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"


[tool.pdm]
distribution = true
